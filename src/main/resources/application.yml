spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/audioworkflow
    username: app
    password: secret

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: localhost
      port: 6379

minio:
  url: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: audio-files